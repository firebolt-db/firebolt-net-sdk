name: Integration tests
on:
  workflow_dispatch:
  workflow_call:
    secrets:
      FIREBOLT_USERNAME_DEV:
        required: true
      FIREBOLT_PASSWORD_DEV:
        required: true
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
    
      - name: Set up .NET 6.0
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 6.0
    
      - name: Install dependencies
        run: |
          dotnet restore

      - name: Setup database and engine
        id: setup
        uses: firebolt-db/integration-testing-setup@master
        with:
          firebolt-username: ${{ secrets.FIREBOLT_USERNAME_DEV }}
          firebolt-password: ${{ secrets.FIREBOLT_PASSWORD_DEV }}
          api-endpoint: "api.dev.firebolt.io"
          region: "us-east-1"

      - name: Run integration tests
        env:
          FIREBOLT_DATABASE: ${{ steps.setup.outputs.database_name }}
          FIREBOLT_USERNAME: ${{ secrets.FIREBOLT_USERNAME_DEV }}
          FIREBOLT_PASSWORD: ${{ secrets.FIREBOLT_PASSWORD_DEV }}
          API_ENDPOINT: "api.dev.firebolt.io"
          ACCOUNT_NAME: "firebolt"
          FIREBOLT_ENGINE_URL: ${{ steps.setup.outputs.engine_url }}
          FIREBOLT_ENGINE_NAME: ${{ steps.setup.outputs.engine_name }}
        run: |
          dotnet test --filter "Category=Integration"        
